server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/taskmanagement
    username: taskuser
    password: taskpass
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
